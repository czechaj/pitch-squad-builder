# Required in production (including Vercel)
DATABASE_URL="file:./prisma/dev.db"

# Mock OTP is enabled by default in non-production, disabled in production.
# Set to true only for controlled testing.
ENABLE_MOCK_OTP=false
